#include <stdlib.h>
#include "libft.h"

/*
t_list *add_link(t_list, char *str);
void    print_list(t_list *list);

int     main(void)
{
    t_list *list;

    list = NULL;

    list = add_link(list, "toto\n");
    print_list(list);

    return (0);
}

*/