-> Identifiy the default gateway in the routing table

-> Command: netstat -nr //to get the routing table, but many data with default tag
-> Command: route -n get default | grep "gateway" | cut -d ' ' -f 6